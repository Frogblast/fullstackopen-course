export const Total = ({ exercises }) => {
    return (
        <div>
            <h2>Total</h2>
            <p>
                Number of exercises: {exercises}
            </p>
        </div>
    );
};
